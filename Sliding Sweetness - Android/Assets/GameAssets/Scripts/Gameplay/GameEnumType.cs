using UnityEngine;

namespace SlidingSweetness
{
    public enum BlockType { Block1, Block2, Block3, Block4, Block5}
    public enum BlockSizeType { Size1, Size2, Size3, Size4 }


    public enum BoardState { Handling, Free }
}